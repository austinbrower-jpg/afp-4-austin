/**
 * @deprecated Prefer importing from `@/types/api`.
 * Re-export kept so existing feature imports continue to resolve.
 */
export type { HoursEntryWithRelations, HoursEntryInput } from "@/types/api";
