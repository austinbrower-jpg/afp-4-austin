/**
 * @deprecated Prefer importing from `@/lib/knowledge/tree`.
 * Re-export kept so existing feature imports continue to resolve.
 */
export {
  buildChildrenMap,
  getDescendantIds,
  validParentOptions,
  getAncestorChain,
} from "@/lib/knowledge/tree";
