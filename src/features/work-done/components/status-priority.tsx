/**
 * @deprecated Prefer importing from `@/components/shared/*`.
 * Thin re-exports so existing work-done feature imports keep working.
 */
export {
  WorkLogStatusBadge as StatusBadge,
  STATUS_OPTIONS,
  STATUS_LABEL,
} from "@/components/shared/work-log-status-badge";
export {
  PriorityBadge,
  PRIORITY_OPTIONS,
  PRIORITY_LABEL,
} from "@/components/shared/priority-badge";
